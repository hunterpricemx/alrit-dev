# Blog / Artículos (CMS, SEO) — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Un blog bilingüe con CMS propio: artículos en Markdown editables desde el admin, listado con filtro por categoría, página de artículo con SEO completo (JSON-LD, OG, hreflang, sitemap), cableado en megamenú/footer/nav-móvil.

**Architecture:** Patrón override+fallback (`safeQuery`). Identidad **id-estable** (patrón Courses) para que renombrar slug no duplique. Server Components cargan datos y los pasan a islas client (BlogFilter, Header). ISR (`revalidate=3600`) + `revalidatePath` on-demand. Markdown renderizado en Server Component con `react-markdown` (sin HTML crudo).

**Tech Stack:** Next.js 16 (App Router, Turbopack), React 19, TypeScript strict, Prisma + Postgres, `react-markdown` + `remark-gfm`, Tailwind v4 + CSS BEM. Todo en Docker.

## Global Constraints

- **Sin tests automatizados**: verificación = `npm run lint` + `npx tsc --noEmit` + `npm run build` + chrome-devtools. Cada tarea cierra con esos checks + commit.
- **`useSearchParams` ⇒ `<Suspense>`** o `next build` FALLA. Por eso `npm run build` es criterio obligatorio en la tarea del listado.
- **Identidad id-estable**: `savePost` hace `update({where:{id}})` si hay id, si no `create()`. NUNCA upsert por slug.
- **Fechas**: loader normaliza `Date→ISO`; `publishedAt` se guarda a mediodía UTC; display con `Intl.DateTimeFormat(locale,{timeZone:"UTC",...})`.
- **hreflang condicional**: emitir alternate `en` solo si `post.hasEn`.
- **Fallbacks**: todo lector usa `safeQuery(fn, [])`; blog vacío → `emptyState`.
- **Header serializable**: `latestPosts` data plana, no filas Prisma.
- **Rutas nuevas**: tras crearlas, `docker compose exec web sh -c "rm -rf .next/*"` + `restart`.
- **Commits** terminan con `Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>`. Admin en ES; sitio ES/EN.

---

## M0 — Cimientos

### Task 1: Esquema `Post` + migración + categorías

**Files:**
- Modify: `prisma/schema.prisma`
- Create: `lib/content/blog-categories.ts`

**Interfaces:**
- Produces: modelo `Post`; `BLOG_CATEGORIES`, `BLOG_CATEGORY_KEYS`, `categoryLabel(key, locale)`.

- [ ] **Step 1: Añadir el modelo Post** (al final de `prisma/schema.prisma`)

```prisma
model Post {
  id          String    @id @default(cuid())
  slug        String    @unique
  category    String
  cover       String?
  author      String    @default("Equipo Alrit")
  locales     Json
  published   Boolean   @default(false)
  publishedAt DateTime?
  createdAt   DateTime  @default(now())
  updatedAt   DateTime  @updatedAt

  @@index([published, publishedAt])
  @@index([category, published, publishedAt])
}
```

- [ ] **Step 2: Crear `lib/content/blog-categories.ts`**

```ts
export type BlogCategory = { key: string; es: string; en: string };

export const BLOG_CATEGORIES: BlogCategory[] = [
  { key: "desarrollo-web", es: "Desarrollo web", en: "Web development" },
  { key: "seo", es: "SEO y posicionamiento", en: "SEO" },
  { key: "ecommerce", es: "E-commerce", en: "E-commerce" },
  { key: "negocio", es: "Negocio digital", en: "Digital business" },
];

export const BLOG_CATEGORY_KEYS = BLOG_CATEGORIES.map((c) => c.key);

export function categoryLabel(key: string, locale: "es" | "en"): string {
  const c = BLOG_CATEGORIES.find((x) => x.key === key);
  return c ? c[locale] : key;
}
```

- [ ] **Step 3: Migración + generate**

```bash
docker compose exec -T web npx prisma migrate dev --name blog
npx prisma generate
```
Expected: "Your database is now in sync" + "Generated Prisma Client".

- [ ] **Step 4: Lint + typecheck**

```bash
npm run lint && npx tsc --noEmit
```

- [ ] **Step 5: Commit**

```bash
git add -A && git commit -m "feat(blog): modelo Post + categorías"
```

---

### Task 2: Dependencia Markdown + componente `Markdown`

**Files:**
- Modify: `package.json` (vía npm i)
- Create: `components/blog/Markdown.tsx`

**Interfaces:**
- Produces: `<Markdown source={string} />` (Server Component).

- [ ] **Step 1: Instalar react-markdown + remark-gfm** (dentro y fuera del contenedor)

```bash
docker compose exec -T web npm i react-markdown remark-gfm
npm i react-markdown remark-gfm
```
Expected: añade ambas a `dependencies`. (Verificado: react-markdown ≥9 peer `react>=18`, compatible con React 19; sin conflicto.)

- [ ] **Step 2: Crear `components/blog/Markdown.tsx`** (Server Component, sin `rehype-raw`)

```tsx
import ReactMarkdown from "react-markdown";
import remarkGfm from "remark-gfm";

export default function Markdown({ source }: { source: string }) {
  return (
    <div className="article__body">
      <ReactMarkdown remarkPlugins={[remarkGfm]}>{source}</ReactMarkdown>
    </div>
  );
}
```

- [ ] **Step 3: Typecheck**

```bash
npx tsc --noEmit
```
Expected: sin errores (tipos de react-markdown resueltos).

- [ ] **Step 4: Commit**

```bash
git add -A && git commit -m "feat(blog): render Markdown (react-markdown + remark-gfm)"
```

---

### Task 3: Loader `lib/content/blog.ts`

**Files:**
- Create: `lib/content/blog.ts`

**Interfaces:**
- Consumes: `db` (Prisma), `safeQuery`.
- Produces: `Post`, `PostLocaleContent`, `getPublishedPostsAsync()`, `getPostBySlugAsync(slug)`, `getAllPostSlugs()` (→ `{slug,updatedAt,publishedAt}[]`), `getLatestPostsAsync(n)`, `postLocale(post,locale)`, `readingTime(body)`.

- [ ] **Step 1: Crear `lib/content/blog.ts`**

```ts
import { db } from "@/lib/db";
import { safeQuery } from "./safe";

export type PostLocaleContent = { title: string; excerpt: string; body: string };

export type Post = {
  slug: string;
  category: string;
  cover: string | null;
  author: string;
  es: PostLocaleContent;
  en: PostLocaleContent;
  published: boolean;
  publishedAt: string | null;
  updatedAt: string;
  hasEn: boolean;
};

export type PostSitemapRow = { slug: string; updatedAt: string; publishedAt: string | null };

type PostRow = {
  slug: string; category: string; cover: string | null; author: string;
  locales: unknown; published: boolean;
  publishedAt: Date | null; updatedAt: Date;
};

const EMPTY: PostLocaleContent = { title: "", excerpt: "", body: "" };

function mapRowToPost(row: PostRow): Post {
  const loc = (row.locales ?? {}) as { es?: PostLocaleContent; en?: PostLocaleContent };
  const es = { ...EMPTY, ...(loc.es ?? {}) };
  const en = { ...EMPTY, ...(loc.en ?? {}) };
  return {
    slug: row.slug,
    category: row.category,
    cover: row.cover,
    author: row.author,
    es, en,
    published: row.published,
    publishedAt: row.publishedAt ? row.publishedAt.toISOString() : null,
    updatedAt: row.updatedAt.toISOString(),
    hasEn: en.title.trim() !== "" && en.body.trim() !== "",
  };
}

const ORDER = { publishedAt: { sort: "desc", nulls: "last" } } as const;

export async function getPublishedPostsAsync(): Promise<Post[]> {
  return safeQuery(async () => {
    const rows = await db.post.findMany({ where: { published: true }, orderBy: ORDER });
    return rows.map(mapRowToPost);
  }, []);
}

export async function getLatestPostsAsync(n: number): Promise<Post[]> {
  return safeQuery(async () => {
    const rows = await db.post.findMany({ where: { published: true }, orderBy: ORDER, take: n });
    return rows.map(mapRowToPost);
  }, []);
}

export async function getPostBySlugAsync(slug: string): Promise<Post | undefined> {
  return safeQuery(async () => {
    const row = await db.post.findUnique({ where: { slug } });
    if (!row || !row.published) return undefined;
    return mapRowToPost(row);
  }, undefined);
}

export async function getAllPostSlugs(): Promise<PostSitemapRow[]> {
  return safeQuery(async () => {
    const rows = await db.post.findMany({
      where: { published: true },
      select: { slug: true, updatedAt: true, publishedAt: true },
    });
    return rows.map((r) => ({
      slug: r.slug,
      updatedAt: r.updatedAt.toISOString(),
      publishedAt: r.publishedAt ? r.publishedAt.toISOString() : null,
    }));
  }, []);
}

export function postLocale(post: Post, locale: "es" | "en"): PostLocaleContent {
  const cur = post[locale];
  const pick = (k: keyof PostLocaleContent) => (cur[k].trim() !== "" ? cur[k] : post.es[k]);
  return { title: pick("title"), excerpt: pick("excerpt"), body: pick("body") };
}

export function readingTime(body: string): number {
  const words = body.trim().split(/\s+/).filter(Boolean).length;
  return Math.max(1, Math.round(words / 200));
}
```

- [ ] **Step 2: Typecheck**

```bash
npx tsc --noEmit
```
Expected: sin errores. (Si el `orderBy` con `nulls` da error de tipo, confirmar `@prisma/client` 6.19 — soporta `{sort,nulls}`.)

- [ ] **Step 3: Commit**

```bash
git add -A && git commit -m "feat(blog): loader con fallback ES/EN, fechas ISO y orden nulls-last"
```

---

### Task 4: Diccionario `blog` + CSS

**Files:**
- Modify: `lib/i18n/dictionaries/es.ts`, `lib/i18n/dictionaries/en.ts`
- Create: `app/blog-x.css`
- Modify: `app/globals.css`

**Interfaces:**
- Produces: `dict.blog.{eyebrow,title,text,allCategories,filterLabel,readMore,relatedTitle,minRead,authorBy,emptyState,backToBlog}`.

- [ ] **Step 1: Añadir bloque `blog` a `es.ts`** (junto a otros bloques de nivel raíz del objeto `es`)

```ts
  blog: {
    eyebrow: "Blog",
    title: "Ideas que hacen crecer tu negocio",
    text: "Guías, casos y aprendizajes sobre desarrollo web, SEO, e-commerce y negocio digital.",
    allCategories: "Todas",
    filterLabel: "Filtrar por categoría",
    readMore: "Leer artículo",
    relatedTitle: "Artículos relacionados",
    minRead: "min de lectura",
    authorBy: "Por",
    emptyState: "Pronto publicaremos contenido aquí. Vuelve pronto.",
    backToBlog: "Volver al blog",
  },
```

- [ ] **Step 2: Añadir el MISMO bloque a `en.ts`** (mismas claves, traducidas)

```ts
  blog: {
    eyebrow: "Blog",
    title: "Ideas that grow your business",
    text: "Guides, case studies and lessons on web development, SEO, e-commerce and digital business.",
    allCategories: "All",
    filterLabel: "Filter by category",
    readMore: "Read article",
    relatedTitle: "Related articles",
    minRead: "min read",
    authorBy: "By",
    emptyState: "We'll be publishing content here soon. Check back soon.",
    backToBlog: "Back to the blog",
  },
```

- [ ] **Step 3: Crear `app/blog-x.css`**

```css
/* ===== Blog ===== */
.blog-hub { max-width: 1200px; margin: 0 auto; padding: 5rem 1.5rem 2rem; }
.blog-hub__eyebrow { font-family: var(--font-display), sans-serif; font-weight: 600; font-size: 0.8rem; letter-spacing: 0.08em; text-transform: uppercase; color: #ff5a3d; text-align: center; }
.blog-hub__title { font-family: var(--font-display), sans-serif; font-weight: 800; font-size: clamp(2rem, 4vw, 3rem); text-align: center; margin: 0.6rem 0 0; }
.blog-hub__sub { max-width: 44rem; margin: 1rem auto 0; text-align: center; color: var(--color-muted); font-size: 1.1rem; line-height: 1.6; }

.blog-filter { display: flex; flex-wrap: wrap; gap: 0.5rem; justify-content: center; margin: 2rem auto; }
.blog-filter__tab { font-family: var(--font-display), sans-serif; font-weight: 600; font-size: 0.9rem; padding: 0.5rem 1rem; border-radius: 999px; border: 1px solid rgba(0,0,0,0.1); background: #fff; color: var(--color-ink); cursor: pointer; transition: all 0.25s ease; }
.blog-filter__tab[aria-current="true"] { background: var(--color-ink); color: #fff; border-color: transparent; }

.blog-grid { max-width: 1200px; margin: 0 auto; padding: 0 1.5rem 5rem; display: grid; grid-template-columns: repeat(auto-fill, minmax(300px, 1fr)); gap: 1.5rem; list-style: none; }
.blog-card { display: flex; flex-direction: column; height: 100%; border-radius: 18px; overflow: hidden; background: #fff; border: 1px solid rgba(0,0,0,0.06); text-decoration: none; transition: transform 0.5s var(--ease-spring), box-shadow 0.4s ease; }
.blog-card:hover { transform: translateY(-6px); box-shadow: 0 22px 44px rgba(0,0,0,0.1); }
.blog-card__media { aspect-ratio: 16/9; background: #eee; overflow: hidden; }
.blog-card__media img { width: 100%; height: 100%; object-fit: cover; }
.blog-card__cat { font-family: var(--font-display), sans-serif; font-weight: 600; font-size: 0.72rem; letter-spacing: 0.04em; text-transform: uppercase; color: #ff5a3d; padding: 1rem 1.1rem 0.3rem; }
.blog-card__title { font-family: var(--font-display), sans-serif; font-weight: 700; font-size: 1.1rem; color: var(--color-ink); padding: 0 1.1rem 0.4rem; }
.blog-card__excerpt { color: var(--color-muted); font-size: 0.92rem; line-height: 1.5; padding: 0 1.1rem 0.8rem; flex: 1; }
.blog-card__meta { display: flex; gap: 0.5rem; color: var(--color-muted); font-size: 0.8rem; padding: 0 1.1rem 1.1rem; }
.blog-empty { max-width: 1200px; margin: 0 auto; padding: 2rem 1.5rem 6rem; text-align: center; color: var(--color-muted); }

/* Artículo */
.article { max-width: 760px; margin: 0 auto; padding: 4rem 1.5rem; }
.article__cat { font-family: var(--font-display), sans-serif; font-weight: 600; font-size: 0.78rem; letter-spacing: 0.05em; text-transform: uppercase; color: #ff5a3d; }
.article__title { font-family: var(--font-display), sans-serif; font-weight: 800; font-size: clamp(2rem, 4vw, 2.8rem); margin: 0.6rem 0; line-height: 1.1; }
.article__meta { display: flex; flex-wrap: wrap; gap: 0.75rem; color: var(--color-muted); font-size: 0.9rem; margin-bottom: 1.5rem; }
.article__cover { width: 100%; border-radius: 18px; margin: 1.5rem 0 2.5rem; }
.article__body { font-size: 1.08rem; line-height: 1.75; color: #2a2a30; }
.article__body h2 { font-family: var(--font-display), sans-serif; font-weight: 700; font-size: 1.6rem; margin: 2.2rem 0 0.8rem; }
.article__body h3 { font-family: var(--font-display), sans-serif; font-weight: 700; font-size: 1.3rem; margin: 1.8rem 0 0.6rem; }
.article__body p { margin: 0 0 1.1rem; }
.article__body ul, .article__body ol { margin: 0 0 1.1rem 1.3rem; }
.article__body li { margin: 0.3rem 0; }
.article__body a { color: #8a6bff; text-decoration: underline; }
.article__body blockquote { border-left: 3px solid #8a6bff; padding-left: 1rem; margin: 1.2rem 0; color: var(--color-muted); font-style: italic; }
.article__body img { max-width: 100%; border-radius: 12px; margin: 1.2rem 0; }
.article__body pre { background: #14141a; color: #f4f4f5; padding: 1rem 1.2rem; border-radius: 12px; overflow-x: auto; margin: 1.2rem 0; font-size: 0.9rem; }
.article__body :not(pre) > code { background: rgba(138,107,255,0.12); color: #6d4bff; padding: 0.1rem 0.4rem; border-radius: 6px; font-size: 0.9em; }
.article__body table { width: 100%; border-collapse: collapse; margin: 1.2rem 0; font-size: 0.95rem; }
.article__body th, .article__body td { border: 1px solid rgba(0,0,0,0.1); padding: 0.5rem 0.75rem; text-align: left; }
```

- [ ] **Step 4: Importar el CSS en `app/globals.css`** (junto a los otros `@import` de `*-x.css`)

```css
@import "./blog-x.css";
```

- [ ] **Step 5: Typecheck (valida que en.ts cumple el tipo)**

```bash
npx tsc --noEmit
```
Expected: sin errores (si faltan claves en `en.ts`, tsc falla con el tipo Dictionary).

- [ ] **Step 6: Commit**

```bash
git add -A && git commit -m "feat(blog): diccionario ES/EN + estilos del blog/artículo"
```

---

## M1 — Admin (id-estable)

### Task 5: Server actions `_actions/blog.ts`

**Files:**
- Create: `app/(admin)/admin/_actions/blog.ts`

**Interfaces:**
- Consumes: `BLOG_CATEGORY_KEYS`.
- Produces: `savePost(prev, formData) → {ok,error?,id?}`, `deletePost(id)`, `togglePublished(id, v)`. `PostFormState`.

- [ ] **Step 1: Crear `app/(admin)/admin/_actions/blog.ts`**

```ts
"use server";

import { z } from "zod";
import { revalidatePath } from "next/cache";
import { db } from "@/lib/db";
import { requireAdmin } from "@/lib/authz";
import { BLOG_CATEGORY_KEYS } from "@/lib/content/blog-categories";

export type PostFormState = { ok: boolean; error?: string; id?: string };

const str = (v: FormDataEntryValue | null) => (typeof v === "string" ? v.trim() : "");

const schema = z.object({
  slug: z.string().min(1).regex(/^[a-z0-9-]+$/, "slug inválido (minúsculas, números y guiones)"),
  category: z.string().refine((k) => BLOG_CATEGORY_KEYS.includes(k), "categoría inválida"),
  cover: z.string(),
  author: z.string().min(1).max(120),
  es_title: z.string().max(110),
  es_excerpt: z.string().max(180),
  es_body: z.string(),
  en_title: z.string().max(110),
  en_excerpt: z.string().max(180),
  en_body: z.string(),
});

function revalidateSite() {
  revalidatePath("/[locale]", "layout");
}

export async function savePost(_prev: PostFormState, formData: FormData): Promise<PostFormState> {
  await requireAdmin();

  const fields = {
    slug: str(formData.get("slug")),
    category: str(formData.get("category")),
    cover: str(formData.get("cover")),
    author: str(formData.get("author")) || "Equipo Alrit",
    es_title: str(formData.get("es_title")),
    es_excerpt: str(formData.get("es_excerpt")),
    es_body: str(formData.get("es_body")),
    en_title: str(formData.get("en_title")),
    en_excerpt: str(formData.get("en_excerpt")),
    en_body: str(formData.get("en_body")),
  };

  const parsed = schema.safeParse(fields);
  if (!parsed.success) return { ok: false, error: parsed.error.issues[0]?.message ?? "Datos inválidos." };

  const id = str(formData.get("id"));
  const published = formData.get("published") === "on";
  const dateInput = str(formData.get("publishedAt"));
  let publishedAt: Date | null = dateInput ? new Date(`${dateInput}T12:00:00Z`) : null;
  if (published && !publishedAt) publishedAt = new Date(`${new Date().toISOString().slice(0, 10)}T12:00:00Z`);

  const data = {
    slug: parsed.data.slug,
    category: parsed.data.category,
    cover: parsed.data.cover || null,
    author: parsed.data.author,
    locales: {
      es: { title: parsed.data.es_title, excerpt: parsed.data.es_excerpt, body: parsed.data.es_body },
      en: { title: parsed.data.en_title, excerpt: parsed.data.en_excerpt, body: parsed.data.en_body },
    },
    published,
    publishedAt,
  };

  try {
    const saved = id
      ? await db.post.update({ where: { id }, data })
      : await db.post.create({ data });
    revalidateSite();
    return { ok: true, id: saved.id };
  } catch {
    return { ok: false, error: "No se pudo guardar (¿slug duplicado?)." };
  }
}

export async function deletePost(id: string): Promise<{ ok: boolean }> {
  await requireAdmin();
  await db.post.delete({ where: { id } });
  revalidateSite();
  return { ok: true };
}

export async function togglePublished(id: string, published: boolean) {
  await requireAdmin();
  await db.post.update({ where: { id }, data: { published } });
  revalidateSite();
}
```

- [ ] **Step 2: Lint + typecheck**

```bash
npm run lint && npx tsc --noEmit
```

- [ ] **Step 3: Commit**

```bash
git add -A && git commit -m "feat(admin/blog): server actions id-estable (save/delete/toggle)"
```

---

### Task 6: Admin blog — lista, editor, form, nav

**Files:**
- Create: `app/(admin)/admin/(protected)/blog/page.tsx`, `new/page.tsx`, `[id]/page.tsx`, `_form.tsx`
- Modify: `app/(admin)/admin/(protected)/_nav.tsx`

**Interfaces:**
- Consumes: `savePost`, `PostFormState`, `BLOG_CATEGORIES`, biblioteca de medios.
- Produces: `PostInitial` (con `id`).

- [ ] **Step 1: Crear `_form.tsx`** (client; espejo de `portfolio/_form.tsx`, con `id` oculto)

```tsx
"use client";

import { useActionState, useState } from "react";
import Link from "next/link";
import { savePost, type PostFormState } from "../../_actions/blog";
import { BLOG_CATEGORIES } from "@/lib/content/blog-categories";

export type PostInitial = {
  id: string; slug: string; category: string; cover: string; author: string;
  publishedAt: string; published: boolean;
  es_title: string; es_excerpt: string; es_body: string;
  en_title: string; en_excerpt: string; en_body: string;
};

type MediaItem = { id: string; url: string; filename: string };

function Field({ label, name, defaultValue, hint }: { label: string; name: string; defaultValue?: string; hint?: string }) {
  return (
    <label className="adm-field">
      <span className="adm-field__label">{label}{hint && <em className="adm-field__hint"> · {hint}</em>}</span>
      <input className="adm-input" name={name} defaultValue={defaultValue} />
    </label>
  );
}

export default function PostForm({ initial, media, isNew }: { initial: PostInitial; media: MediaItem[]; isNew: boolean }) {
  const [state, formAction, pending] = useActionState<PostFormState, FormData>(savePost, { ok: false });
  const [cover, setCover] = useState(initial.cover);

  return (
    <form action={formAction} className="adm-panel adm-form">
      <input type="hidden" name="id" defaultValue={initial.id} />
      <div className="adm-form__grid">
        <Field label="Slug (URL)" name="slug" defaultValue={initial.slug} hint="minúsculas-con-guiones" />
        <label className="adm-field">
          <span className="adm-field__label">Categoría</span>
          <select className="adm-input" name="category" defaultValue={initial.category || BLOG_CATEGORIES[0].key}>
            {BLOG_CATEGORIES.map((c) => <option key={c.key} value={c.key}>{c.es}</option>)}
          </select>
        </label>
        <Field label="Autor" name="author" defaultValue={initial.author} />
        <Field label="Fecha de publicación" name="publishedAt" defaultValue={initial.publishedAt} hint="AAAA-MM-DD" />
        <div className="adm-field">
          <span className="adm-field__label">Portada</span>
          <input className="adm-input" name="cover" value={cover} onChange={(e) => setCover(e.target.value)} placeholder="/ruta o URL" />
          {media.length > 0 && (
            <select className="adm-input" value="" onChange={(e) => e.target.value && setCover(e.target.value)}>
              <option value="">— biblioteca —</option>
              {media.map((m) => <option key={m.id} value={m.url}>{m.filename}</option>)}
            </select>
          )}
          {/* eslint-disable-next-line @next/next/no-img-element */}
          {cover && <img className="adm-slot__img" src={cover} alt="" />}
        </div>
      </div>

      <div className="adm-form__cols">
        <fieldset className="adm-fieldset">
          <legend>Español</legend>
          <Field label="Título" name="es_title" defaultValue={initial.es_title} />
          <Field label="Extracto" name="es_excerpt" defaultValue={initial.es_excerpt} />
          <label className="adm-field">
            <span className="adm-field__label">Cuerpo (Markdown)</span>
            <textarea className="adm-input adm-textarea" name="es_body" rows={16} defaultValue={initial.es_body} />
          </label>
        </fieldset>
        <fieldset className="adm-fieldset">
          <legend>English</legend>
          <Field label="Title" name="en_title" defaultValue={initial.en_title} />
          <Field label="Excerpt" name="en_excerpt" defaultValue={initial.en_excerpt} />
          <label className="adm-field">
            <span className="adm-field__label">Body (Markdown)</span>
            <textarea className="adm-input adm-textarea" name="en_body" rows={16} defaultValue={initial.en_body} />
          </label>
        </fieldset>
      </div>

      <label className="adm-check">
        <input type="checkbox" name="published" defaultChecked={initial.published} /> Publicado
      </label>

      <div className="adm-actions">
        <button className="adm-btn" type="submit" disabled={pending}>{pending ? "Guardando…" : isNew ? "Crear" : "Guardar cambios"}</button>
        <Link href="/admin/blog" className="adm-btn adm-btn--ghost">Volver</Link>
        {state.ok && <span className="adm-ok">Guardado ✓</span>}
        {state.error && <span className="adm-err">{state.error}</span>}
      </div>
    </form>
  );
}
```

- [ ] **Step 2: Crear `page.tsx`** (lista, enlaza por **id**)

```tsx
import Link from "next/link";
import { db } from "@/lib/db";
import { safeQuery } from "@/lib/content/safe";
import { categoryLabel } from "@/lib/content/blog-categories";

export const dynamic = "force-dynamic";

export default async function BlogAdminPage() {
  const posts = await safeQuery(() => db.post.findMany({ orderBy: { updatedAt: "desc" } }), []);
  return (
    <>
      <header className="adm__head">
        <h1 className="adm__title">Blog</h1>
        <p className="adm__subtitle">Artículos del blog (Markdown, bilingüe).</p>
        <Link href="/admin/blog/new" className="adm-btn">Nuevo artículo</Link>
      </header>
      <div className="adm-panel">
        {posts.length === 0 ? <p className="adm-row__hint">Aún no hay artículos.</p> : (
          <ul className="adm-list">
            {posts.map((p) => {
              const loc = (p.locales as { es?: { title?: string } })?.es;
              return (
                <li key={p.id} className="adm-list__row">
                  <Link href={`/admin/blog/${p.id}`} className="adm-list__link">{loc?.title || p.slug}</Link>
                  <span className="adm-list__meta">{categoryLabel(p.category, "es")} · {p.published ? "Publicado" : "Borrador"}</span>
                </li>
              );
            })}
          </ul>
        )}
      </div>
    </>
  );
}
```
(Si `.adm-list`/`.adm-list__row`/`.adm-list__link`/`.adm-list__meta` no existen, reusar las clases de la lista de `/admin/portfolio` que sí existan — verificar en `portfolio/page.tsx` y copiar esas clases.)

- [ ] **Step 3: Crear `new/page.tsx`**

```tsx
import { db } from "@/lib/db";
import { safeQuery } from "@/lib/content/safe";
import PostForm, { type PostInitial } from "../_form";

export const dynamic = "force-dynamic";

const EMPTY: PostInitial = {
  id: "", slug: "", category: "", cover: "", author: "Equipo Alrit",
  publishedAt: "", published: false,
  es_title: "", es_excerpt: "", es_body: "", en_title: "", en_excerpt: "", en_body: "",
};

export default async function NewPostPage() {
  const media = await safeQuery(() => db.media.findMany({ orderBy: { createdAt: "desc" } }), []);
  return (
    <>
      <header className="adm__head"><h1 className="adm__title">Nuevo artículo</h1></header>
      <PostForm initial={EMPTY} media={media} isNew />
    </>
  );
}
```

- [ ] **Step 4: Crear `[id]/page.tsx`** (lee por **id**, incluye borrar)

```tsx
import { notFound, redirect } from "next/navigation";
import { db } from "@/lib/db";
import { safeQuery } from "@/lib/content/safe";
import { deletePost } from "../../../_actions/blog";
import PostForm, { type PostInitial } from "../_form";

export const dynamic = "force-dynamic";

type Loc = { title?: string; excerpt?: string; body?: string };

export default async function EditPostPage({ params }: { params: Promise<{ id: string }> }) {
  const { id } = await params;
  const p = await safeQuery(() => db.post.findUnique({ where: { id } }), null);
  if (!p) notFound();
  const loc = (p.locales as { es?: Loc; en?: Loc }) ?? {};
  const media = await safeQuery(() => db.media.findMany({ orderBy: { createdAt: "desc" } }), []);

  const initial: PostInitial = {
    id: p.id, slug: p.slug, category: p.category, cover: p.cover ?? "", author: p.author,
    publishedAt: p.publishedAt ? p.publishedAt.toISOString().slice(0, 10) : "",
    published: p.published,
    es_title: loc.es?.title ?? "", es_excerpt: loc.es?.excerpt ?? "", es_body: loc.es?.body ?? "",
    en_title: loc.en?.title ?? "", en_excerpt: loc.en?.excerpt ?? "", en_body: loc.en?.body ?? "",
  };

  return (
    <>
      <header className="adm__head"><h1 className="adm__title">Editar artículo</h1><p className="adm__subtitle">{p.slug}</p></header>
      <PostForm initial={initial} media={media} isNew={false} />
      <form className="adm-danger" action={async () => { "use server"; await deletePost(id); redirect("/admin/blog"); }}>
        <button type="submit" className="adm-danger__btn">Eliminar artículo</button>
      </form>
    </>
  );
}
```

- [ ] **Step 5: Link "Blog" en `_nav.tsx`** (ICONS + entrada entre Logos y Textos)

```ts
  // en ICONS:
  blog: "M4 4h16v16H4zM8 8h8M8 12h8M8 16h5",
```
```ts
  // en NAV, después de Logos:
  { href: "/admin/blog", label: "Blog", icon: ICONS.blog, ready: true },
```

- [ ] **Step 6: Lint + typecheck + build**

```bash
npm run lint && npx tsc --noEmit && npm run build
```
Expected: build OK con rutas `/admin/blog`, `/admin/blog/new`, `/admin/blog/[id]`.

- [ ] **Step 7: Limpiar cache + restart + verificar (ruta nueva)**

```bash
docker compose exec web sh -c "rm -rf .next/*" && docker compose restart web
```
En chrome-devtools: `/admin/blog` → "Nuevo artículo" → llenar slug, categoría, título/extracto/cuerpo ES (Markdown con un `## titulo` y una lista), publicar, guardar. Editar el slug y reguardar → confirmar que sigue el mismo registro (no duplica). Crear un 2º post solo-ES en borrador.

- [ ] **Step 8: Commit**

```bash
git add -A && git commit -m "feat(admin/blog): lista, editor id-estable, form Markdown + nav"
```

---

## M2 — Blog público + SEO

### Task 7: `/blog` (listado + filtro + Suspense + metadata)

**Files:**
- Create: `app/(site)/[locale]/blog/page.tsx`, `components/blog/BlogFilter.tsx`

**Interfaces:**
- Consumes: `getPublishedPostsAsync`, `BLOG_CATEGORIES`, `categoryLabel`, `postLocale`, `readingTime`, `dict.blog`.

- [ ] **Step 1: Crear `components/blog/BlogFilter.tsx`** (client, valida `?cat=`)

```tsx
"use client";

import { useState } from "react";
import Link from "next/link";
import { useSearchParams } from "next/navigation";
import { BLOG_CATEGORIES, BLOG_CATEGORY_KEYS, categoryLabel } from "@/lib/content/blog-categories";

export type BlogCard = {
  slug: string; category: string; cover: string | null;
  title: string; excerpt: string; date: string; minutes: number;
};

export default function BlogFilter({ cards, locale, t }: {
  cards: BlogCard[]; locale: "es" | "en";
  t: { allCategories: string; minRead: string; emptyState: string; filterLabel: string };
}) {
  const sp = useSearchParams();
  const raw = sp.get("cat");
  const initial = raw && BLOG_CATEGORY_KEYS.includes(raw) ? raw : "all";
  const [cat, setCat] = useState(initial);
  const shown = cat === "all" ? cards : cards.filter((c) => c.category === cat);

  return (
    <>
      <div className="blog-filter" role="tablist" aria-label={t.filterLabel}>
        <button type="button" className="blog-filter__tab" aria-current={cat === "all"} onClick={() => setCat("all")}>{t.allCategories}</button>
        {BLOG_CATEGORIES.map((c) => (
          <button key={c.key} type="button" className="blog-filter__tab" aria-current={cat === c.key} onClick={() => setCat(c.key)}>{c[locale]}</button>
        ))}
      </div>
      {shown.length === 0 ? (
        <p className="blog-empty">{t.emptyState}</p>
      ) : (
        <ul className="blog-grid">
          {shown.map((c) => (
            <li key={c.slug}>
              <Link href={`/${locale}/blog/${c.slug}`} className="blog-card">
                {c.cover && (
                  <span className="blog-card__media">
                    {/* eslint-disable-next-line @next/next/no-img-element */}
                    <img src={c.cover} alt={c.title} loading="lazy" />
                  </span>
                )}
                <span className="blog-card__cat">{categoryLabel(c.category, locale)}</span>
                <span className="blog-card__title">{c.title}</span>
                <span className="blog-card__excerpt">{c.excerpt}</span>
                <span className="blog-card__meta">{c.date && <span>{c.date}</span>}<span>{c.minutes} {t.minRead}</span></span>
              </Link>
            </li>
          ))}
        </ul>
      )}
    </>
  );
}
```

- [ ] **Step 2: Crear `app/(site)/[locale]/blog/page.tsx`** (ISR + metadata propio + Suspense)

```tsx
import { Suspense } from "react";
import type { Metadata } from "next";
import { isLocale, type Locale, locales } from "@/lib/i18n/config";
import { getDictionaryAsync } from "@/lib/i18n";
import { getPublishedPostsAsync, postLocale, readingTime } from "@/lib/content/blog";
import BlogFilter, { type BlogCard } from "@/components/blog/BlogFilter";
import { notFound } from "next/navigation";

export const revalidate = 3600;
const SITE_URL = "https://alrit.dev";

export function generateStaticParams() {
  return locales.map((locale) => ({ locale }));
}

export async function generateMetadata({ params }: { params: Promise<{ locale: string }> }): Promise<Metadata> {
  const { locale } = await params;
  const l = isLocale(locale) ? locale : "es";
  const dict = await getDictionaryAsync(l);
  return {
    title: `${dict.blog.eyebrow} | Alrit.dev`,
    description: dict.blog.text,
    alternates: {
      canonical: `/${l}/blog`,
      languages: { es: "/es/blog", en: "/en/blog", "x-default": "/es/blog" },
    },
    openGraph: { title: `${dict.blog.eyebrow} | Alrit.dev`, description: dict.blog.text, url: `${SITE_URL}/${l}/blog`, type: "website" },
  };
}

function fmtDate(iso: string | null, l: Locale): string {
  if (!iso) return "";
  return new Intl.DateTimeFormat(l === "en" ? "en-US" : "es-MX", { timeZone: "UTC", day: "2-digit", month: "long", year: "numeric" }).format(new Date(iso));
}

export default async function BlogPage({ params }: { params: Promise<{ locale: string }> }) {
  const { locale } = await params;
  if (!isLocale(locale)) notFound();
  const l = locale as Locale;
  const [dict, posts] = await Promise.all([getDictionaryAsync(l), getPublishedPostsAsync()]);

  const cards: BlogCard[] = posts.map((p) => {
    const c = postLocale(p, l);
    return { slug: p.slug, category: p.category, cover: p.cover, title: c.title, excerpt: c.excerpt, date: fmtDate(p.publishedAt, l), minutes: readingTime(c.body) };
  });

  return (
    <>
      <header className="blog-hub">
        <p className="blog-hub__eyebrow">{dict.blog.eyebrow}</p>
        <h1 className="blog-hub__title">{dict.blog.title}</h1>
        <p className="blog-hub__sub">{dict.blog.text}</p>
      </header>
      <Suspense fallback={<div className="blog-grid" />}>
        <BlogFilter cards={cards} locale={l} t={{ allCategories: dict.blog.allCategories, minRead: dict.blog.minRead, emptyState: dict.blog.emptyState, filterLabel: dict.blog.filterLabel }} />
      </Suspense>
    </>
  );
}
```

- [ ] **Step 3: Lint + typecheck + build (Suspense crítico)**

```bash
npm run lint && npx tsc --noEmit && npm run build
```
Expected: build OK. **Si falla con "Missing Suspense boundary with useSearchParams", el `<Suspense>` no está envolviendo BlogFilter** — corregir.

- [ ] **Step 4: Cache + restart + verificar**

```bash
docker compose exec web sh -c "rm -rf .next/*" && docker compose restart web
```
chrome-devtools: `/es/blog` lista solo el post publicado; tabs de categoría filtran; `/es/blog?cat=desarrollo-web` arranca filtrado; `/es/blog?cat=xxx` cae a "Todas".

- [ ] **Step 5: Commit**

```bash
git add -A && git commit -m "feat(blog): listado /blog con filtro (Suspense) y metadata propio"
```

---

### Task 8: `/blog/[slug]` (artículo + relacionados + metadata)

**Files:**
- Create: `app/(site)/[locale]/blog/[slug]/page.tsx`

**Interfaces:**
- Consumes: `getPostBySlugAsync`, `getAllPostSlugs`, `getPublishedPostsAsync`, `postLocale`, `readingTime`, `Markdown`, `categoryLabel`, `BlogPostingJsonLd`, `BreadcrumbJsonLd`.

- [ ] **Step 1: Crear `app/(site)/[locale]/blog/[slug]/page.tsx`**

```tsx
import type { Metadata } from "next";
import Link from "next/link";
import { notFound } from "next/navigation";
import { isLocale, type Locale, locales } from "@/lib/i18n/config";
import { getDictionaryAsync } from "@/lib/i18n";
import { getPostBySlugAsync, getAllPostSlugs, getPublishedPostsAsync, postLocale, readingTime } from "@/lib/content/blog";
import { categoryLabel } from "@/lib/content/blog-categories";
import { BlogPostingJsonLd, BreadcrumbJsonLd } from "@/lib/seo/jsonld";
import Markdown from "@/components/blog/Markdown";

export const revalidate = 3600;
const SITE_URL = "https://alrit.dev";

export async function generateStaticParams() {
  const rows = await getAllPostSlugs();
  return locales.flatMap((locale) => rows.map((r) => ({ locale, slug: r.slug })));
}

function plain(md: string): string {
  return md.replace(/[#>*_`~\-]/g, " ").replace(/\s+/g, " ").trim();
}

export async function generateMetadata({ params }: { params: Promise<{ locale: string; slug: string }> }): Promise<Metadata> {
  const { locale, slug } = await params;
  const l = isLocale(locale) ? locale : "es";
  const post = await getPostBySlugAsync(slug);
  if (!post) return {};
  const c = postLocale(post, l);
  const description = (c.excerpt || plain(c.body)).slice(0, 155);
  const languages: Record<string, string> = { es: `/es/blog/${slug}`, "x-default": `/es/blog/${slug}` };
  if (post.hasEn) languages.en = `/en/blog/${slug}`;
  const cover = post.cover ? (post.cover.startsWith("http") ? post.cover : `${SITE_URL}${post.cover}`) : undefined;
  return {
    title: `${c.title} | Alrit.dev`,
    description,
    alternates: { canonical: `/${l}/blog/${slug}`, languages },
    openGraph: {
      title: c.title, description, type: "article", url: `${SITE_URL}/${l}/blog/${slug}`,
      locale: l === "en" ? "en_US" : "es_MX",
      publishedTime: post.publishedAt ?? undefined, modifiedTime: post.updatedAt,
      images: cover ? [cover] : undefined,
    },
    twitter: { card: "summary_large_image", title: c.title, description },
  };
}

function fmtDate(iso: string | null, l: Locale): string {
  if (!iso) return "";
  return new Intl.DateTimeFormat(l === "en" ? "en-US" : "es-MX", { timeZone: "UTC", day: "2-digit", month: "long", year: "numeric" }).format(new Date(iso));
}

export default async function PostPage({ params }: { params: Promise<{ locale: string; slug: string }> }) {
  const { locale, slug } = await params;
  if (!isLocale(locale)) notFound();
  const l = locale as Locale;
  const post = await getPostBySlugAsync(slug);
  if (!post) notFound();

  const [dict, all] = await Promise.all([getDictionaryAsync(l), getPublishedPostsAsync()]);
  const c = postLocale(post, l);
  const related = all.filter((p) => p.category === post.category && p.slug !== post.slug).slice(0, 3);
  const cover = post.cover ? (post.cover.startsWith("http") ? post.cover : `${SITE_URL}${post.cover}`) : undefined;
  const base = `/${l}`;

  return (
    <>
      <BlogPostingJsonLd
        url={`${SITE_URL}/${l}/blog/${slug}`} headline={c.title.slice(0, 110)}
        description={(c.excerpt || c.title).slice(0, 155)} image={cover}
        datePublished={post.publishedAt} dateModified={post.updatedAt}
        author={post.author} locale={l}
      />
      <BreadcrumbJsonLd items={[
        { name: "Inicio", url: `${SITE_URL}${base}` },
        { name: dict.blog.eyebrow, url: `${SITE_URL}${base}/blog` },
        { name: c.title, url: `${SITE_URL}${base}/blog/${slug}` },
      ]} />

      <article className="article">
        <Link href={`${base}/blog`} className="blog-card__cat" style={{ padding: 0 }}>← {dict.blog.backToBlog}</Link>
        <p className="article__cat">{categoryLabel(post.category, l)}</p>
        <h1 className="article__title">{c.title}</h1>
        <div className="article__meta">
          <span>{dict.blog.authorBy} {post.author}</span>
          {post.publishedAt && <span>· {fmtDate(post.publishedAt, l)}</span>}
          <span>· {readingTime(c.body)} {dict.blog.minRead}</span>
        </div>
        {cover && (
          /* eslint-disable-next-line @next/next/no-img-element */
          <img className="article__cover" src={cover} alt={c.title} />
        )}
        <Markdown source={c.body} />
      </article>

      {related.length > 0 && (
        <section className="blog-hub" style={{ paddingTop: 0 }}>
          <h2 className="blog-hub__title" style={{ fontSize: "1.6rem" }}>{dict.blog.relatedTitle}</h2>
          <ul className="blog-grid" style={{ padding: "1.5rem 0 4rem" }}>
            {related.map((p) => {
              const rc = postLocale(p, l);
              return (
                <li key={p.slug}>
                  <Link href={`${base}/blog/${p.slug}`} className="blog-card">
                    {p.cover && <span className="blog-card__media">{/* eslint-disable-next-line @next/next/no-img-element */}<img src={p.cover} alt={rc.title} loading="lazy" /></span>}
                    <span className="blog-card__cat">{categoryLabel(p.category, l)}</span>
                    <span className="blog-card__title">{rc.title}</span>
                    <span className="blog-card__excerpt">{rc.excerpt}</span>
                  </Link>
                </li>
              );
            })}
          </ul>
        </section>
      )}
    </>
  );
}
```

- [ ] **Step 2: Build (depende del JSON-LD de la Task 9 — orden)**

> NOTA DE ORDEN: este page importa `BlogPostingJsonLd`, que se crea en la Task 9. Hacer la Task 9 ANTES del build/verify de esta, o crear primero el stub del componente. Recomendado: implementar Task 9 Step 1 (añadir el componente a jsonld.tsx) inmediatamente antes del build de esta tarea.

- [ ] **Step 3: Cache + restart + verificar**

```bash
docker compose exec web sh -c "rm -rf .next/*" && docker compose restart web
```
chrome-devtools: `/es/blog/<slug>` renderiza Markdown (h2, lista, código, enlace, imagen), autor/fecha/lectura, relacionados; el post borrador → 404; `/en/blog/<slug-solo-es>` muestra ES (fallback).

- [ ] **Step 4: Commit**

```bash
git add -A && git commit -m "feat(blog): página de artículo con Markdown, relacionados y metadata SEO"
```

---

### Task 9: SEO — JSON-LD + sitemap + robots

**Files:**
- Modify: `lib/seo/jsonld.tsx`, `app/sitemap.ts`, `app/robots.ts`

**Interfaces:**
- Produces: `BlogPostingJsonLd` (consumido por Task 8).

- [ ] **Step 1: Añadir `BlogPostingJsonLd` a `lib/seo/jsonld.tsx`** (mismo estilo que los existentes; `publisher` por `@id`)

```tsx
export function BlogPostingJsonLd({
  url, headline, description, image, datePublished, dateModified, author, locale,
}: {
  url: string; headline: string; description: string; image?: string;
  datePublished: string | null; dateModified: string; author: string; locale: "es" | "en";
}) {
  const data: Record<string, unknown> = {
    "@context": "https://schema.org",
    "@type": "BlogPosting",
    headline,
    description,
    datePublished: datePublished ?? dateModified,
    dateModified,
    author: { "@type": "Person", name: author },
    publisher: { "@id": "https://alrit.dev/#organization" },
    mainEntityOfPage: url,
    inLanguage: locale === "en" ? "en-US" : "es-MX",
  };
  if (image) data.image = image;
  return <script type="application/ld+json" dangerouslySetInnerHTML={{ __html: JSON.stringify(data) }} />;
}
```
(Confirmar el patrón exacto de `<script type="application/ld+json">` mirando cómo lo hacen `OrganizationJsonLd`/`CaseStudyJsonLd` y copiarlo idéntico.)

- [ ] **Step 2: Añadir posts a `app/sitemap.ts`** (lastModified por post)

```ts
import { getAllPostSlugs } from "@/lib/content/blog";
// ... dentro de sitemap():
  const posts = await getAllPostSlugs();
  const blogEntries: MetadataRoute.Sitemap = posts.flatMap((p) =>
    locales.map((locale) => ({
      url: `${SITE_URL}/${locale}/blog/${p.slug}`,
      lastModified: new Date(p.updatedAt),
      changeFrequency: "monthly" as const,
      priority: locale === "es" ? 0.6 : 0.55,
      alternates: { languages: { es: `${SITE_URL}/es/blog/${p.slug}`, en: `${SITE_URL}/en/blog/${p.slug}` } },
    }))
  );
// ... y en el return añadir:
    ...entry("/blog", "weekly", 0.7),
    ...blogEntries,
```

- [ ] **Step 3: `app/robots.ts` — disallow /admin**

Editar las reglas para añadir `disallow: "/admin"` (manteniendo `allow: "/"`). Verificar la forma actual del archivo y añadir el campo `disallow` al objeto de `rules`.

- [ ] **Step 4: Build + verificar SEO**

```bash
npm run build
```
chrome-devtools: en `/es/blog/<slug>` inspeccionar `<head>` (canonical `/es/blog/<slug>`, hreflang es + en solo si hasEn, og:type article); el `<script type="application/ld+json">` BlogPosting es JSON válido; `curl http://localhost:3000/sitemap.xml` incluye `/blog` y el post; `curl http://localhost:3000/robots.txt` muestra Disallow /admin.

- [ ] **Step 5: Commit**

```bash
git add -A && git commit -m "feat(blog): JSON-LD BlogPosting + sitemap por post + robots disallow admin"
```

---

### Task 10: Cableado — megamenú, nav móvil, footer

**Files:**
- Modify: `components/layout/Header.tsx`, `app/(site)/[locale]/layout.tsx`, `components/layout/Footer.tsx`

**Interfaces:**
- Consumes: `getLatestPostsAsync`, `BLOG_CATEGORIES`, `categoryLabel`.
- Produces: `Header` recibe `latestPosts: {slug,title,cover,category}[]`.

- [ ] **Step 1: Layout carga últimos posts y los pasa al Header**

En `app/(site)/[locale]/layout.tsx`:
```ts
import { getLatestPostsAsync, postLocale } from "@/lib/content/blog";
// dentro de LocaleLayout, junto a la carga de `featured`:
  const latest = await getLatestPostsAsync(3);
  const latestPosts = latest.map((p) => ({ slug: p.slug, title: postLocale(p, locale as Locale).title, cover: p.cover, category: p.category }));
// en el render:
  <Header dict={dict} locale={locale as Locale} featured={featuredFallback} latestPosts={latestPosts} />
```

- [ ] **Step 2: Header — tipo + panel blog + drawer móvil**

En `components/layout/Header.tsx`:
```ts
import { BLOG_CATEGORIES, categoryLabel } from "@/lib/content/blog-categories";
type LatestPost = { slug: string; title: string; cover: string | null; category: string };
// firma:
export default function Header({ dict, locale, featured, latestPosts }: { dict: Dictionary; locale: Locale; featured: FeaturedItem[]; latestPosts: LatestPost[] }) {
```
Reemplazar el bloque `open === "blog"` (el de `m.blogCats`/`mega-soon`) por:
```tsx
{open === "blog" && (
  <div className="mega mega--portfolio">
    <p className="mega__group">{dict.nav.blog}</p>
    <div className="mega__cases">
      {latestPosts.map((p) => (
        <Link key={p.slug} href={`${base}/blog/${p.slug}`} className="mega-case">
          {p.cover && <span className="mega-case__media">{/* eslint-disable-next-line @next/next/no-img-element */}<img src={p.cover} alt={p.title} loading="lazy" /></span>}
          <span className="mega-case__cat">{categoryLabel(p.category, locale)}</span>
          <span className="mega-case__name">{p.title}</span>
        </Link>
      ))}
    </div>
    <div className="mega__col">
      {BLOG_CATEGORIES.map((c) => (
        <Link key={c.key} href={`${base}/blog?cat=${c.key}`} className="mega-link">
          <span className="mega-link__body"><span className="mega-link__name">{c[locale]}</span></span>
        </Link>
      ))}
      <Link href={`${base}/blog`} className="mega__all">{dict.nav.blog}
        <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" strokeWidth="2" strokeLinecap="round" strokeLinejoin="round" aria-hidden="true"><path d="M5 12h14M13 6l6 6-6 6" /></svg>
      </Link>
    </div>
  </div>
)}
```
En el drawer móvil, añadir (entre Cursos y Calculadora):
```tsx
<Link href={`${base}/blog`} className="drawer__sec" onClick={() => setMobileOpen(false)}>{dict.nav.blog}</Link>
```

- [ ] **Step 3: Footer — link Blog** (columna "company", reusa la clave existente)

En `components/layout/Footer.tsx`, junto a portfolio/process/servicios:
```tsx
<Link href={`${base}/blog`} className="footer__link">{dict.footer.company.blog}</Link>
```
(Verificar el nombre de la clase de link y de `base` en el Footer y usar los existentes.)

- [ ] **Step 4: Lint + typecheck + build**

```bash
npm run lint && npx tsc --noEmit && npm run build
```

- [ ] **Step 5: Cache + restart + verificar**

```bash
docker compose exec web sh -c "rm -rf .next/*" && docker compose restart web
```
chrome-devtools (hard reload por bundle del Header): hover "Blog" en el megamenú → categorías + últimos posts; click en categoría → `/blog?cat=` filtrado; footer con link Blog; en 390px el drawer móvil tiene Blog.

- [ ] **Step 6: Commit**

```bash
git add -A && git commit -m "feat(blog): cableado megamenú + nav móvil + footer"
```

---

## Cierre

- Tras M0–M2: el blog es navegable (megamenú/footer/móvil), indexable (JSON-LD/sitemap/hreflang), editable (admin id-estable, Markdown bilingüe).
- **Limpieza**: dejar los posts demo en borrador o eliminarlos (producción arranca vacía con `emptyState`).
- Actualizar memoria `alrit-admin-lms-payments` con "Blog HECHO" + commits.
- **Revisión final (ultracode)**: tras implementar, correr un workflow adversarial que revise el blog implementado (render Markdown real, head/JSON-LD/sitemap reales, fallback /en, responsive) antes de cerrar.
- Fuera de alcance (v1): comentarios, RSS, editor visual, resaltado de sintaxis, tags, búsqueda, logo real del publisher.
